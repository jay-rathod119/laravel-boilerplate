# Railway (Nixpacks): Laravel document root + PHP extensions
# ext-intl / ext-zip are in composer.json "require" so Nixpacks installs them.
# Start command (uses $PORT) is in railway.toml for Railway.
[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
