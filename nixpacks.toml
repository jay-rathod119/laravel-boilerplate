# Railway (Nixpacks): Laravel document root + PHP extensions
# ext-intl / ext-zip are also declared in composer.json "require" so Nixpacks installs them.
[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
